# Prefix used by MiraCore chat output.
prefix: "&5[Mira]&r "

# Runs a compact self-check shortly after startup and writes the result to console.
diagnostics:
  startup-check: true

# Shared suite audit history.
audit:
  max-entries: 5000
